{
  "spec_version": "1.4",
  "case_id": "atomic-cycle-engine-base-multivenue-canary",
  "design_status": "approved_bounded_live_canary_active",
  "evidence_level": "repository_record_plus_live_runtime_chain_and_management_receipts",
  "live_execution_authorized": true,
  "objective": {
    "statement": "Measure and maximize risk-adjusted net return for a same-chain 2-4 hop atomic cycle while preserving state identity, authorization, settlement truth, and a complete opportunity denominator.",
    "risk_budget": {
      "max_input_native": 0.003,
      "min_signer_reserve_native": 0.005,
      "cumulative_failed_gas_cap_native": 0.001,
      "max_concurrent_nonce_lanes": 1,
      "structural_bound": "Base chain 8453 only; WETH settlement; canonical Uniswap V2, Uniswap V3 and PancakeSwap V3 two-pool cycles; ten-token allowlist; no arbitrary target, calldata, approval, delegatecall, or residual intermediate position."
    },
    "success_semantics": {
      "shadow_candidate": "Every hop and gas conversion share one state commitment and net profit remains above the proposed floor after all modeled costs.",
      "live_success": "A RECONCILED_SUCCESS EffectRecord proves canonical receipt success, route event identity, positive executor WETH delta, known L2 gas, Base L1 data fee and operator fee, economic net above the approved floor, and FLAT residual exposure.",
      "unknown": "Any missing state, quote, coverage, receipt, balance, cost, or causal attribution remains UNKNOWN rather than inferred."
    }
  },
  "opportunity": {
    "kind": "same_chain_state_snapshot_atomic_cycle",
    "definition": "A revisioned immutable state snapshot proving a 2-4 hop route from one settlement asset back to itself with positive modeled net profit; events and websites only wake evaluation.",
    "identity_fields": [
      "chain_id",
      "settlement_asset",
      "ordered_liquidity_source_ids",
      "ordered_adapter_ids",
      "amount_in",
      "state_basis",
      "state_reference",
      "state_commitment"
    ],
    "dedupe_key": "chain_id:settlement_asset:route_id:amount_in:state_basis:state_reference:state_commitment",
    "revision_policy": "A new block, flashblock sequence, pool state commitment, amount, route, or risk policy creates a new Opportunity and never mutates the prior action chain.",
    "atomicity": "One same-chain transaction must begin and end in the same settlement asset and may not reuse one mutable liquidity source."
  },
  "timing": {
    "clock": "chain block or pending sequence plus monotonic local timestamps",
    "activation": {
      "basis": "STATE",
      "origin": "immutable opportunity snapshot",
      "absolute_value": "all required pool states, gas conversion, route identity, and policy commitment are known",
      "behavior": "Before all state, contract, authorization, balance and cost commitments are known the candidate remains read-only. After activation it may prepare and sign once, then broadcast the identical raw transaction before the composite boundary expires."
    },
    "boundary": {
      "termination_rule": "ANY",
      "refresh_policy": "never_extend_same_opportunity",
      "constraints": [
        {
          "id": "state_revision",
          "basis": "STATE",
          "origin": "frozen pool dependency set",
          "absolute_value": "every hop and gas quote retains the exact same state commitment"
        },
        {
          "id": "canonical_replacement",
          "basis": "BLOCK",
          "origin": "frozen canonical block",
          "absolute_value": "the block remains canonical and has not been replaced"
        },
        {
          "id": "pending_sequence_replacement",
          "basis": "SEQUENCE",
          "origin": "frozen flashblock sequence when pending state is used",
          "absolute_value": "no later sequence supersedes the dependency commitment"
        }
      ]
    },
    "deadline_behavior": "Stop evaluating the old action chain, mark the reason, and create a new opportunity revision only from a newly frozen state; continue evidence reconciliation.",
    "deadline_checkpoints": [
      "before_route_evaluation",
      "before_each_quote",
      "before_gas_conversion",
      "before_full_simulation",
      "before_sign",
      "before_broadcast"
    ],
    "timeline_fields": [
      "source_occurred_at",
      "received_at",
      "normalized_at",
      "snapshot_frozen_at",
      "decided_at",
      "signed_at",
      "broadcast_started_at"
    ]
  },
  "profile": {
    "strategy": "generic_same_chain_atomic_cycle",
    "operation_mode": "live",
    "network_scope": ["base-mainnet"],
    "route_scope": "WETH-token-WETH using any two distinct canonical pools across Uniswap V2, Uniswap V3 and PancakeSwap V3 with a ten-token capital allowlist",
    "runtime_readback": "Base production is active on executor 0x002ccD95D1304C6fB88f67183DC1e7d1b90A577F with 0.003 WETH, armed=true, both fixed profit floors at one wei, active systemd PID and a current heartbeat; no arbitrage broadcast or RECONCILED_SUCCESS exists yet"
  },
  "decision": {
    "correctness_invariants": [
      {
        "id": "same_state_commitment",
        "condition": "Every hop quote and gas conversion has the exact chain, state basis, reference, and commitment frozen by the Opportunity.",
        "risk_reduced": "Prevents fictitious profit assembled from different market states.",
        "false_block_cost": "One potentially valid candidate is omitted until a consistent snapshot exists.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "KNOWN",
          "p95_ms": 0,
          "basis": "Phase 0 performs an in-memory equality check."
        },
        "voi_estimate": {
          "state": "KNOWN",
          "expected_loss_reduction": "Eliminates mixed-state false positives from the eligible set.",
          "basis": "Correctness invariant."
        },
        "blocking_mode": "block",
        "isolation_scope": "opportunity",
        "fallback": "record BLOCKED_CORRECTNESS and wait for a new state revision"
      },
      {
        "id": "atomic_cycle_shape",
        "condition": "The path is same-chain, contains 2-4 contiguous swaps, starts and ends in the declared settlement asset, and never reuses mutable liquidity.",
        "risk_reduced": "Prevents hidden inventory, cross-chain finality risk, and double-counted pool liquidity.",
        "false_block_cost": "Unsupported longer or non-atomic paths remain research-only.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "KNOWN",
          "p95_ms": 0,
          "basis": "Phase 0 validates route structure in memory."
        },
        "voi_estimate": {
          "state": "KNOWN",
          "expected_loss_reduction": "Keeps the modeled payoff inside one atomic settlement boundary.",
          "basis": "Correctness invariant."
        },
        "blocking_mode": "block",
        "isolation_scope": "opportunity",
        "fallback": "reject the route"
      },
      {
        "id": "orthogonal_source_attribution",
        "condition": "Discovery source, launch platform, launch protocol, and liquidity venue are stored as independent evidence-backed claims.",
        "risk_reduced": "Prevents a discovery API such as PAIR from being misreported as the platform or venue for unrelated assets such as NINECAT.",
        "false_block_cost": "Unattributed pools remain UNKNOWN until chain evidence is available.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "KNOWN",
          "p95_ms": 0,
          "basis": "Phase 0 validates the normalized object locally."
        },
        "voi_estimate": {
          "state": "KNOWN",
          "expected_loss_reduction": "Prevents wrong protocol adapters and product labels.",
          "basis": "Observed historical attribution defect."
        },
        "blocking_mode": "block",
        "isolation_scope": "capability",
        "fallback": "keep the affected source discovery-only"
      },
      {
        "id": "bounded_live_scope",
        "condition": "Signing requires the dedicated Base signer, deployed executor readback, manual authorization id, runtime arm, on-chain arm, approved token, immutable 0.003 WETH principal cap, 0.005 ETH signer reserve and unresolved-attempt-free nonce lane.",
        "risk_reduced": "Prevents a research adapter, wrong wallet, wrong contract, stale approval or concurrent nonce owner from moving funds.",
        "false_block_cost": "One otherwise profitable cycle is missed until every capital-bearing commitment is restored.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "KNOWN",
          "p95_ms": 0,
          "basis": "Runtime policy tests, credential permission tests, nonce-fence tests and on-chain executor state validation."
        },
        "voi_estimate": {
          "state": "KNOWN",
          "expected_loss_reduction": "Caps one trade principal and cumulative known failed gas while isolating signing to the approved Base canary.",
          "basis": "Authorization and capital-bound invariant."
        },
        "blocking_mode": "block",
        "isolation_scope": "wallet_lane",
        "fallback": "do not sign and continue read-only scanning"
      }
    ],
    "adaptive_gates": [
      {
        "id": "all_costs_known",
        "condition": "Pool fees, protocol costs, gas conversion, financing fees when applicable, and risk buffer are known at the same state.",
        "risk_reduced": "Prevents gross spread from being mislabeled as net profit.",
        "false_block_cost": "A real opportunity may remain unranked while a cost adapter is unavailable.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "UNKNOWN",
          "reason": "The Base three-venue adapter is implemented, but production p95 latency and public-RPC tail behavior are not yet measured."
        },
        "voi_estimate": {
          "state": "UNKNOWN",
          "reason": "Avoided false-positive and missed-edge distributions require live opportunity and canonical outcome evidence."
        },
        "blocking_mode": "degrade",
        "isolation_scope": "capability",
        "fallback": "record the candidate as research-only and exclude it from eligible shadow profit"
      },
      {
        "id": "coverage_complete",
        "condition": "The chainwide census watermark is gap-free through the required confirmed block or pending sequence.",
        "risk_reduced": "Prevents opportunity-frequency claims from an unknown denominator.",
        "false_block_cost": "Business-rate conclusions wait for backfill while individual candidates may still be evaluated.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "UNKNOWN",
          "reason": "Backfill and reorg performance are Phase 1 measurements."
        },
        "voi_estimate": {
          "state": "KNOWN",
          "expected_loss_reduction": "Stops incomplete scans from justifying infrastructure or capital expansion.",
          "basis": "Denominator integrity."
        },
        "blocking_mode": "degrade",
        "isolation_scope": "capability",
        "fallback": "publish coverage as partial and suppress opportunity-rate conclusions"
      }
    ],
    "soft_checks": [
      {
        "id": "external_candidate_corrobation",
        "condition": "Third-party indexers or platform APIs corroborate a chain-derived pool without delaying local evaluation.",
        "risk_reduced": "Improves metadata and operator discovery context.",
        "false_block_cost": "None because corroboration is not an execution gate.",
        "latency_budget_ms": 0,
        "latency_estimate": {
          "state": "UNKNOWN",
          "reason": "External API latency is not measured."
        },
        "voi_estimate": {
          "state": "UNKNOWN",
          "reason": "No causal execution value has been established."
        },
        "blocking_mode": "async",
        "isolation_scope": "capability",
        "fallback": "continue with chain evidence only"
      }
    ]
  },
  "execution": {
    "mode": "guarded_atomic_profit",
    "profile": "bounded_base_multivenue_live_canary",
    "min_out": {
      "mode": "atomic_profit_floor",
      "formula": "amountOut = amountIn + grossProfit and grossProfit >= max(contract floor, maximum L2 gas plus buffered Base L1 data fee plus buffered operator fee plus approved minimum net profit)",
      "live_effect": true
    },
    "authorization": {
      "mode": "MANUAL_ARM",
      "scope_binding": "ADR 0002 plus ADR 0006 bind Base chain 8453, the dedicated signer, WETH settlement, canonical Uniswap V2/V3 and PancakeSwap V3, token allowlist, 0.003 WETH principal, 0.005 ETH reserve, 0.001 ETH failed-gas cap and per-opportunity immutable signed plan.",
      "expiry_policy": "Manual disarm or the earliest deadline, valid-through block, policy gate, failed-gas fuse, unknown receipt, disputed effect or reserve violation.",
      "evidence_record": "Authorization id is persisted in each append-only PLAN record and deployment scope is recorded in ADR 0002.",
      "live_effect": true
    },
    "nonce_policy": "One persisted local nonce owner; an unresolved signed PLAN blocks new risk.",
    "signing_policy": "Sign exactly one immutable EIP-1559 raw transaction after full-call simulation and all policy checks.",
    "broadcast_policy": "Fan out only that same raw transaction to one to four endpoints; never create endpoint-specific replacements for the same attempt.",
    "rpc_policy": "Public Base RPC is the initial read and broadcast path; authenticated providers require separate economic justification."
  },
  "recovery": {
    "receipt_timeout": "Persist UNKNOWN and halt the wallet lane until the same transaction hash is reconciled against canonical chain state.",
    "single_rpc_null_policy": "never terminal",
    "restart_policy": "Acquire the nonce fence and reconcile any unresolved signed PLAN before scanning or signing a new opportunity.",
    "kill_switch": "Stop the watcher, wait for its nonce fence to release, then submit setArmed(false); stale execute plans still expire by deadline and valid-through block."
  },
  "exit": {
    "policy": "Every successful transaction is atomically FLAT in WETH; there is no non-atomic inventory exit path.",
    "atomic_success": "The intermediate token balance equals its pre-trade balance and all proceeds remain WETH in the executor.",
    "residual": "The contract reverts on any intermediate-token balance difference; any contradictory receipt or balance observation becomes DISPUTED and blocks the wallet lane.",
    "live_residual_recovery_authorized": false
  },
  "evidence": {
    "levels": [
      "configured",
      "repository_record",
      "live_observed",
      "receipt_attested",
      "economic_reconciled"
    ],
    "append_only_events": [
      "pool_fact_observed",
      "coverage_updated",
      "opportunity_observed",
      "quote_recorded",
      "shadow_decision_recorded",
      "counterfactual_outcome_recorded",
      "reconciliation_corrected"
    ],
    "current_runtime_verified": true,
    "services_or_transactions_run_this_case": true,
    "effect_record_required_for_live_claim": true,
    "secret_material_permitted": false
  },
  "capabilities": {
    "transport": {
      "level": "TESTED",
      "access": "WRITE",
      "limitations": [
        "Same-raw HTTP fanout is implemented and public Base RPC reads were observed; production provider ack latency and Flashblocks transport are not yet verified."
      ]
    },
    "discovery": {
      "level": "PLANNED",
      "access": "READ",
      "limitations": [
        "Contract registries exist, but gap-free factory and PoolManager backfills are not implemented."
      ]
    },
    "identity": {
      "level": "TESTED",
      "access": "READ",
      "limitations": [
        "Source dimensions and registry invariants have unit tests; full live pool attribution is not implemented."
      ]
    },
    "quote": {
      "level": "TESTED",
      "access": "READ",
      "limitations": [
        "Canonical Base Uniswap V2 reserve math plus Uniswap and PancakeSwap V3 QuoterV2 exact quotes are tested and live-read. Robinhood Earn/Balancer V3 pool quotes, reviewed Earn/DEX hybrids, and curated BNB 3-hop best-of-venue paths are fixed-block read-only capabilities; their new mixed and BNB calldata paths remain unsupported."
      ]
    },
    "simulation": {
      "level": "TESTED",
      "access": "READ",
      "limitations": [
        "Deterministic contract tests and a current Base mainnet fork exercise both directions through the on-chain profit gate; third-party audit and broad non-standard-token fuzzing are not complete."
      ]
    },
    "calldata": {
      "level": "TESTED",
      "access": "PREPARE",
      "limitations": [
        "Only the typed three-venue BaseV2V3CycleExecutor route is supported; arbitrary and generic multi-hop calldata remain unsupported."
      ]
    },
    "sign": {
      "level": "TESTED",
      "access": "WRITE",
      "limitations": [
        "Root-only or systemd credential loading and immutable transaction signing are implemented; receipt-attested deployment and migration used the production signer, while no Base arbitrage payload has been broadcast yet."
      ]
    },
    "broadcast": {
      "level": "IMPLEMENTED",
      "access": "WRITE",
      "limitations": [
        "Same-raw fanout is active with two verified public Base endpoints; management transactions have canonical receipts, while the Base arbitrage payload path remains unexercised because no candidate has cleared the full live gate."
      ]
    },
    "reconcile": {
      "level": "TESTED",
      "access": "RECOVER",
      "limitations": [
        "Nonce fencing, durable PLAN/BROADCAST/UNKNOWN/EFFECT stages, canonical receipt/event/WETH/L1-data-fee/operator-fee checks and restart blocking are implemented; management receipts exist but no production arbitrage receipt exists yet."
      ]
    },
    "exit": {
      "level": "TESTED",
      "access": "RECOVER",
      "limitations": [
        "Atomic residual rejection and operator disarm/withdraw-while-disarmed are tested; no non-atomic token position recovery is authorized."
      ]
    },
    "replay": {
      "level": "TESTED",
      "access": "READ",
      "limitations": [
        "Deterministic synthetic execution and current Base fork compatibility are covered; historical opportunity and competitor replay remain planned."
      ]
    }
  },
  "race_thesis": {
    "reward_source": "Transient disagreement between executable AMM paths; the first state-changing capture is economically funded by the pre-existing pool imbalance after LP fees, protocol costs, gas, and financing.",
    "allocation": "EXCLUSIVE_STATE_CLAIM",
    "sequencing_rule": {
      "type": "FEE_PRIORITY",
      "evidence_state": "KNOWN",
      "evidence_refs": [
        "https://docs.optimism.io/op-stack/protocol/differences#transactions",
        "https://docs.base.org/base-chain/api-reference/ethereum-json-rpc-api/eth_maxPriorityFeePerGas"
      ],
      "fee_curve": "Use the current EIP-1559 max-priority-fee estimate without replacement bidding, subject to a 1 gwei max-fee ceiling; fee priority influences inclusion but does not bypass the atomic positive-profit floor."
    },
    "earliest_public_signal": "A canonical or pending pool-state change, liquidity change, or new pool event observable from a chain endpoint.",
    "earliest_inferable_signal": "A locally maintained dependency graph predicts that one changed pool may create a cycle before full-graph scanning.",
    "earliest_safe_action": "The Base typed multi-venue route has an exact quote, complete L2 plus L1 data fee plus operator fee bound, immutable identity, full executor simulation, signer reserve, nonce ownership and an atomic on-chain profit floor.",
    "controllable_edges": [
      "INFORMATION",
      "DECISION",
      "PREPARATION",
      "TRANSPORT",
      "FEE",
      "ECONOMIC"
    ],
    "invalidating_evidence": [
      "Canonical same-state replay shows no positive net cycle after all costs.",
      "A competitor consistently captures the exact opportunity before the proposed path can safely submit.",
      "Observed Hook or token semantics break atomic settlement or exact quote assumptions.",
      "Opportunity capacity is below the minimum size required to pay gas."
    ]
  },
  "signal_strategy": {
    "ladder": [
      {
        "id": "canonical_state_change",
        "tier": "STATE",
        "source": "Factory, PoolManager, pool, and canonical block events with a local dependency graph.",
        "causal_claim": "A changed dependency can alter one or more atomic cycle outputs; the event itself does not prove profit.",
        "known_fields": ["chain_id", "block_or_sequence", "pool_identity", "state_commitment"],
        "late_binding_fields": ["exact_amount", "gas_cost", "risk_buffer"]
      },
      {
        "id": "base_flashblock_state",
        "tier": "STATE",
        "source": "Base Flashblocks pending state and pending logs.",
        "causal_claim": "A newer sequencer state can reveal a transient cycle earlier than a finalized block, subject to sequence replacement and coverage proof.",
        "known_fields": ["chain_id", "flashblock_sequence", "changed_dependencies"],
        "late_binding_fields": ["canonical_inclusion", "exact_amount", "gas_cost"]
      },
      {
        "id": "external_candidate_hint",
        "tier": "RESULT",
        "source": "PAIR, Long, DexScreener, GeckoTerminal, or an aggregator response.",
        "causal_claim": "The source can prioritize a cold-path lookup but cannot establish platform identity, completeness, or executable profit.",
        "known_fields": ["source_identity", "candidate_asset_or_pool"],
        "late_binding_fields": ["chain_provenance", "venue", "state", "route", "net_profit"]
      }
    ],
    "chosen_primary": "canonical_state_change",
    "fallbacks": ["base_flashblock_state", "external_candidate_hint"],
    "late_binding_fields": ["amount_in", "gas_conversion", "risk_policy", "competition_state"],
    "false_positive_policy": "A signal only creates an Opportunity candidate; mixed state, missing costs, unsafe semantics, or non-positive net value fail closed without a shot.",
    "false_negative_audit": "Compare the gap-free chainwide census with observed candidates and later canonical profitable state changes; classify NOT_OBSERVED separately."
  },
  "competition": {
    "type": "EXCLUSIVE_STATE_CLAIM",
    "competitor_model": "UNKNOWN-v0: competitors may use private endpoints, colocated infrastructure, custom simulators, priority fees, or pre-positioned inventory; no global win-rate assumption is allowed.",
    "win_probability_dimensions": [
      "chain",
      "venue_set",
      "hop_count",
      "signal_tier",
      "opportunity_lifetime_bucket",
      "net_profit_bucket",
      "submission_path",
      "fee_policy"
    ],
    "ordering_evidence": [
      "OP Stack documentation states that sequencer tx-pool execution uses highest priority fee first; Base documents the EIP-1559 priority fee as a tip paid to its sequencer. Same-fee tie behavior and competitor private paths remain unknown and are measured from canonical positions."
    ],
    "sample_policy": {
      "minimum_total": 30,
      "insufficient_behavior": "Report exact counts and an interval; do not claim a stable win probability or authorize profit-mode execution."
    }
  },
  "shot_policy": {
    "profiles": [
      {
        "id": "base-multivenue-positive-net-only",
        "profile": "REACTIVE_GUARDED",
        "race_mode": "PROFIT",
        "reason": "Sign only after exact quote, buffered full-cost positive net, full executor simulation, fresh block boundary and all wallet/contract risk gates."
      },
      {
        "id": "all-other-routes",
        "profile": "NO_SHOT",
        "race_mode": "SHADOW",
        "reason": "Protocols, tokens and route shapes outside the Base three-venue canary remain no-shot in this engine. Four exact Earn-only routes have a separate typed keeper, but that independent authorization does not grant mixed Earn/DEX or BNB execution here."
      }
    ],
    "race_experiment": {
      "enabled": false,
      "reason": "The watcher never submits a loss-seeking probe; every live attempt must satisfy the profit shot profile, so no attempt-count experiment budget is used."
    }
  },
  "race_timeline": {
    "required_boundaries": [
      "source_occurred_at",
      "received_at",
      "state_frozen_at",
      "quote_completed_at",
      "decision_completed_at",
      "competitor_state_change_at"
    ],
    "wire_measurement_level": "UNKNOWN",
    "inclusion_position_required": true
  },
  "opportunity_census": {
    "source": "Gap-free canonical Factory, PoolManager, and pool-state history, with Base pending sequences maintained as a separate non-final view.",
    "completeness_watermark": "Per source: required_from, observed_through, canonical_head, confirmation_depth, explicit gaps, reorg corrections, and latest pending sequence.",
    "counterfactual_policy": "Replay each chainwide candidate with the adapter and policy version valid at that state; never use a later price or gas value without labeling the counterfactual.",
    "required_outcomes": [
      "NOT_OBSERVED",
      "OBSERVED_AFTER_EXPIRY",
      "OBSERVED",
      "NO_SHOT_NEGATIVE_EV",
      "NO_SHOT_POLICY",
      "BLOCKED_CORRECTNESS",
      "BLOCKED_RESOURCE",
      "ATTEMPTED",
      "WON",
      "CONFIRMED_LOST_RACE",
      "LOW_VALUE_GUARD_EXIT",
      "INVALID_OPPORTUNITY_EXIT",
      "SHARED_CAPTURE",
      "UNKNOWN"
    ]
  },
  "learning": {
    "race_denominator": ["WON", "CONFIRMED_LOST_RACE"],
    "excluded_from_race": [
      "NOT_OBSERVED",
      "NO_SHOT_NEGATIVE_EV",
      "NO_SHOT_POLICY",
      "BLOCKED_CORRECTNESS",
      "BLOCKED_RESOURCE",
      "LOW_VALUE_GUARD_EXIT",
      "INVALID_OPPORTUNITY_EXIT",
      "UNKNOWN"
    ],
    "model_versioning": "Append-only adapter, cost, policy, competitor, and opportunity-identity versions; corrections supersede but never delete prior evidence."
  }
}
